package com.surajverma.notevault

data class ProfileModel(
    var name: String ? = "",
    var collegeName: String ? = "",
    var enrollment: String ? = "",
    var branch: String ?= "",
    var year: String ?= "",
    var phone: String ?= ""
)
